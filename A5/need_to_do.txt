additional headers in the MTP

need to write garbage collector

Error bounding - ENOTBOUND (while sendto)

how to check dest IP and dest port as we are sending it anywhere

use memcpy instead of strcpy

check ENOBUFS, binding -> what to display exactly, ENOMSG, ENOBUFS -> sendto

check the deletion and addition of new socket in every iteration of R

Add the flag nospace