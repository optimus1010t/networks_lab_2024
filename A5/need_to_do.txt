{{{ use memcpy instead of strcpy }}}

vary p and report

make the documentation.txt

for the duplicated messages also, it sends an ACK message to the sender with the sequence number of the last in-order message received and the updated rwnd size.
t updates the swnd accordingly (slides the window till the last message acknowledged if not a duplicate ACK)

check ENOBUFS, binding -> what to display exactly, ENOMSG, ENOBUFS, ENOTBOUND and len issues -> sendto

check the deletion and addition of new socket in every iteration of R

check the flag nospace



additional headers in the MTP --> do we even need them 